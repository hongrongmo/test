package org.ei.stripes;

import net.sourceforge.stripes.controller.StripesFilter;
/**
 * Extending the StripesFilter class to handle some EV-specific tasks
 * 
 * @author harovetm
 *
 */
public class EVStripesFilter extends StripesFilter {

	@Override
	public void destroy() {
	}

}
