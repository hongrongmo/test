<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">

<log4j:configuration debug="false"
	xmlns:log4j="http://jakarta.apache.org/log4j/">
	<appender name="CA" class="org.apache.log4j.ConsoleAppender">
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%d %-5p (%F:%L) - %m%n" />
		</layout>
	</appender>

	<appender name="RFA" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="File" value="${catalina.base}/logs/application.log" />
		<param name="DatePattern" value="'.'yyyy-MM-dd" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%d %-5p (%F:%L) - %m%n" />
		</layout>
	</appender>
	
	<appender name="CARSERRORRFA" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="File" value="${catalina.base}/logs/carserrors.log" />
		<param name="DatePattern" value="'.'yyyy-MM-dd" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%d %-5p (%F:%L) - %m%n" />
		</layout>
	</appender>

	<!-- Perf4J appenders -->
	<!-- This AsyncCoalescingStatisticsAppender groups StopWatch log messages 
		into GroupedTimingStatistics messages which it sends on the file appender 
		defined below -->
	<appender name="PERF4JCOALESCE"
		class="org.perf4j.log4j.AsyncCoalescingStatisticsAppender">
		<!-- The TimeSlice option is used to determine the time window for which 
			all received StopWatch logs are aggregated to create a single GroupedTimingStatistics 
			log. Here we set it to 10 seconds, overriding the default of 30000 ms -->
		<param name="TimeSlice" value="10000" />
		<!-- <appender-ref ref="PERF4JSTATS" />  -->
        <appender-ref ref="SearchExecutionTimes"/>
        <appender-ref ref="SearchResultsExecutionTimes"/>
        <appender-ref ref="SearchResultsCounts"/>
        <appender-ref ref="RequestTPS"/>
	</appender>

	<!-- This file appender is used to output aggregated performance statistics -->
	<!-- CURRENTLY NOT USED!!  NOTE that the graphing appenders work IN MEMORY  -->
	<!-- so they don't require this appender! -->
	<!-- 
	<appender name="PERF4JSTATS" class="org.apache.log4j.DailyRollingFileAppender">
		<param name="File" value="${catalina.base}/logs/perfstats.log" />
		<param name="DatePattern" value="'.'yyyy-MM-dd" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%m%n" />
		</layout>
	</appender>
 	-->
 
    <appender name="SearchExecutionTimes"
              class="org.perf4j.log4j.GraphingStatisticsAppender">
        <!-- Possible GraphTypes are Mean, Min, Max, StdDev, Count and TPS -->
        <param name="GraphType" value="Mean"/>
        <!-- The tags of the timed execution blocks to graph are specified here -->
        <param name="TagNamesToGraph" value="SEARCH_SUBMIT"/>
    </appender>
    <appender name="SearchResultsExecutionTimes"
              class="org.perf4j.log4j.GraphingStatisticsAppender">
        <!-- Possible GraphTypes are Mean, Min, Max, StdDev, Count and TPS -->
        <param name="GraphType" value="Mean"/>
        <!-- The tags of the timed execution blocks to graph are specified here -->
        <param name="TagNamesToGraph" value="QUICK_SEARCH_RESULTS,EXPERT_SEARCH_RESULTS,THES_SEARCH_RESULTS,DEDUP_SEARCH_RESULTS,PATENTREF_SEARCH_RESULTS,OTHERREF_SEARCH_RESULTS,COMBINE_SEARCH_RESULTS"/>
    </appender>
    <appender name="SearchResultsCounts"
              class="org.perf4j.log4j.GraphingStatisticsAppender">
        <!-- Possible GraphTypes are Mean, Min, Max, StdDev, Count and TPS -->
        <param name="GraphType" value="Count"/>
        <!-- The tags of the timed execution blocks to graph are specified here -->
        <param name="TagNamesToGraph" value="QUICK_SEARCH_RESULTS,EXPERT_SEARCH_RESULTS,THES_SEARCH_RESULTS,DEDUP_SEARCH_RESULTS,PATENTREF_SEARCH_RESULTS,OTHERREF_SEARCH_RESULTS,COMBINE_SEARCH_RESULTS"/>
    </appender>
    <appender name="RequestTPS"
              class="org.perf4j.log4j.GraphingStatisticsAppender">
        <param name="GraphType" value="TPS"/>
        <param name="TagNamesToGraph" value="request"/>
    </appender>
    
	<!-- Loggers -->
	<!-- The Perf4J logger. Note that org.perf4j.TimingLogger is the value of 
		the org.perf4j.StopWatch.DEFAULT_LOGGER_NAME constant. Also, note that additivity 
		is set to false, which is usually what is desired - this means that timing 
		statements will only be sent to this logger and NOT to upstream loggers. -->
	<logger name="org.perf4j.TimingLogger" additivity="false">
		<level value="INFO" />
		<appender-ref ref="PERF4JCOALESCE" />
	</logger>

	<!-- ******************************************************************** -->
	<!-- Root logger for EV.  Defaults to "WARN" but the application may      -->
	<!-- change it based on runtime properties.                               -->
	<!-- See EVInitializationListener for more details!                       -->
	<!-- ******************************************************************** -->
	<root>
        <level value="WARN" />
        <appender-ref ref="RFA" />
        <appender-ref ref="CA" />
	</root>
	<logger name="CarsErrorLogger" additivity="false">
		<level value="INFO" />
		<appender-ref ref="CARSERRORRFA" />
	</logger>
</log4j:configuration>